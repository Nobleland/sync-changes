command /test [<text>]:
    trigger:
        if arg 1 is "1":
            execute console command "give Kornphatthanan minecraft:diamond 1"
        else if arg 1 is "2":
            execute console command "say Hello, world!"
